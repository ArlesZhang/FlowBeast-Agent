Read and fully understand `[FILE_PATH]` before taking any action.

Follow the file as the source of truth and execute the task strictly according to its instructions, architecture, constraints, and goals.

Before implementation:
- understand the intent
- identify affected modules/files
- check compatibility with the existing system
- follow existing project conventions and rules (CLAUDE.md / cursor rules / project architecture)

Do NOT:
- skip important requirements
- invent unrelated architecture
- hardcode temporary solutions
- ignore existing patterns

If anything is unclear or conflicts with the current system, ask before proceeding.

After completion:
- summarize what was done
- explain how the implementation follows the file requirements

File:
`[FILE_PATH]`
